import React from 'react'
import Entry from '../Registration'

const Layout = (params) => {
    return (
        <div className="layout">
            This is layout
            <Entry> </Entry>
        </div>
    )
}

export default Layout